# Container tools

### info

  - podman
  - skopeo
  - buildah

### dnf-install
```sh
sudo dnf install -y podman skopeo buildah
```

### apt-install
```sh
sudo apt-get update
sudo apt install -y podman skopeo buildah
```

### default install
