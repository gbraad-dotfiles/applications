name: test-app-actions
run-name: Test app install with project builds

on:
  workflow_dispatch:
    inputs:
      runs-on:
        description: "Target runner"
        required: true
        type: string
        default: "ubuntu-latest"
      arch:
        description: "Target architecture (e.g., arm64, amd64)"
        required: true
        type: string
        default: "amd64"
  workflow_call:
    inputs:
      runs-on:
        description: "Target runner"
        required: true
        type: string
        default: "ubuntu-latest"
      arch:
        description: "Target architecture (e.g., arm64, amd64)"
        required: true
        type: string
        default: "amd64"

jobs:
  test-project-builds:
    runs-on: ${{ inputs.runs-on }}${{ inputs.arch == 'arm64' && '-arm' || '' }}

    steps:
      - name: Enable linger and create user session (workaround)
        uses: gbraad-actions/blacksmith-user-session@main

      - name: Allow unprivileged userns (workaround)
        uses: gbraad-actions/warpbuild-unprivileged-userns@main

      # This folder will be used with the container
      - name: Create userdirs
        run: |
          cd ${{ github.workspace }}
          mkdir -p Projects
          ln -s ${{ github.workspace }}/Projects  ${HOME}/Projects

      - name: Setup environment
        uses: gbraad-dotfiles/install-action@main
        with:
          upstream: true

      - name: Run app command - checkout projects/crc
        uses: gbraad-dotfiles/app-action@main
        with:
          appname: projects/crc
          action: source checkout

      - name: Run app command - checkout projects/macadam
        uses: gbraad-dotfiles/app-action@main
        with:
          appname: projects/macadam
          action: source checkout

      # separate checkout and compile due to docker permissions
      - name: Run app command - compile projects/crc
        uses: gbraad-dotfiles/app-action@main
        with:
          appname: projects/crc
          action: compile

      - name: Run app command - compile projects/macadam
        uses: gbraad-dotfiles/app-action@main
        with:
          appname: projects/macadam
          action: compile


